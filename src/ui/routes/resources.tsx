import Fusion from "@rbxts/fusion";
import { Paragraph } from "ui/components/foundational/paragraph";
import { Scoped } from "ui/scoped";

export function Resources({ scope }: Scoped) {
	return <Paragraph scope={scope} text="Resources" />;
}
